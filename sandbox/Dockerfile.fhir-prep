# Use a lightweight Python image for the prep container
FROM python:3.10-slim

# Set working directory
WORKDIR /app

# Install required dependencies
RUN pip install pyyaml requests

# Copy the preparation script into the container
COPY app/prep.py /app/prep.py

# Run the preparation script
CMD ["python", "/app/prep.py"]
